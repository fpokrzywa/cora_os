# New Session Handoff Brief

> **Detailed architecture, build state, environment, database, design decisions, and the full
> module-by-module changelog now live in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md).**
> This brief keeps only the concise summary of what's done plus the forward-looking guidance a new
> session needs (next steps, do-not-repeat rules, assumptions, first message, compact summary).

## Project

**Cora AI OS** — a governed multi-agent assistant + AI operating system on self-hosted infra
(Ubuntu Server, Docker, Postgres/pgvector, Redis, n8n, DGX Spark). Cora is the user persona;
ATLAS is the internal orchestrator; specialist subagents are SCRIBE (memory), FORGE (engineering),
PULSE (research/web search), SIGNAL (communication), CHRONOS (time/schedule). Non-autonomous by
design: deterministic routers + manual triggers + admin actions only; everything auditable.

Repo `/home/owner/cora-ai-os/` (cora-api, cora-ui, cora-worker, mcp-filesystem, mcp-placeholder);
infra compose `/home/owner/cora-stack/` (n8n + redis). Stack deployed and healthy as of 2026-06-12.

## What's Completed (concise summary)

Full detail per module is in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9. In brief, the following are shipped and verified:

- **Platform foundation** — JWT auth (first user = admin bootstrap); idempotent schema migrations on every cora-api start; pgvector cutover done; scoped data (user/global/system + workspace_id, NULL = legacy-visible); Redis; worker with `FOR UPDATE SKIP LOCKED` job queue + heartbeat + retries; boot-race pool-init resilience (v2.7.1).
- **Agents & routing** — Cora persona / ATLAS orchestrator / SCRIBE·FORGE·PULSE·SIGNAL·CHRONOS subagents; deterministic keyword routing (`app/agents/routing.py`); DB-backed versioned prompts via Agent Admin (activate/archive/history) + an Agent Test Harness; current-date/time preamble + workspace-context injection.
- **Memory** — scoped + workspace-aware hybrid retrieval (semantic + keyword merge); chunked embeddings (`memory_entry_chunks`, 4000/400) preferred over memory-level vectors; content_hash dedup; SCRIBE summarization; embeddings via Ollama `nomic-embed-text` (768d).
- **Tools / governance / MCP** — tool registry with `allowed_agents` + policy table + risk levels + rate limits; full audit (`tool_execution_logs` + `runtime_traces`); HTTP JSON-RPC MCP layer with working filesystem MCP.
- **Plans / jobs / delegations / traces / workspaces** — template-based execution plans (no auto-exec); worker-executed jobs; ATLAS-only delegations (max 3 active, no self-delegation); runtime traces on every action; workspace selector + context injection.
- **Knowledge ingestion** — unified `knowledge_sources` + linked `memory_entries` (content_hash dedup, scope-aware): manual/bulk/file-upload/URL (HTML/plain/PDF)/URL-refresh; news ingestion via `news_ingest.py` (`news_feed`/`news_article`) with optional full-article-body fetch, registered feeds, background scheduled refresh (`news_feed_refresh` job + 60s scheduler tick, exponential backoff, scheduler-health on `/worker/health`); News Feed Manager + News Briefing UI. Old `news_sources`/`/news/*` path removed in v2.6 (DB table remains, droppable).
- **PULSE web search** — live `web_search` tool backed by self-hosted SearXNG (`cora-searxng` on `cora-proxy` for egress).
- **SIGNAL / CHRONOS governed planning (internal-only, review-only)** — `communication_drafts` / `schedule_proposals` + review workflow (draft/proposed → in_review → reviewed → approved → archived; approve requires admin); chat-to-draft creation behind the governance gate; full review/audit trails. "Approved" is internal only — never sends/schedules.
- **External execution readiness (all non-executing, fail-closed)** — Integration Readiness intents (v0.4) → provider connectors registry (v0.5) → OAuth credential vault + real Fernet encryption (v0.6) → real OAuth flow connect/callback/refresh (v1.1) → provider execution framework + adapters (v1.0/v1.6) → execution approval console (v1.4) → final safety interlock (v1.5) → feature-flag matrix (v1.7) → execution governance dashboard (v1.8). A global kill switch (`EXTERNAL_EXECUTION_ENABLED=false`, v0.8) is the master gate; no Gmail/Outlook/Google/Microsoft execution call is ever made.
- **Chat-native email ops (v1.9–v2.4)** — manage the full SIGNAL email lifecycle, governance explanations, provider-payload simulation, and the approval queue from the chat box; read-only inbox capability aligned (gmail/outlook `supports_read=true`, send still hard-disabled).
- **Live inbox read (v2.7)** — real read-only Gmail/Graph adapters + a token broker behind the fail-closed gate; production stays denied until the operator adds a read scope (`gmail.readonly`/`Mail.Read`) and enables the per-provider `inbox_read` flag. Read-only never implies send (no write method exists).
- **Screen Context Awareness v0.1** (2026-06-12) — chat answers "what am I looking at?" from the same records the screen renders (Tier-1 structured context, no screenshots; entities re-resolved server-side under the caller's ownership).
- **Tier-2 Screen Vision v0.1** (latest, 2026-06-26) — opt-in visual layer on Screen Context: a "Share screen" composer button (browser `getDisplayMedia`, single frame, no auto-capture) → a LOCAL DGX vision model (Ollama `images` param). Fail-closed behind the dedicated `SCREEN_VISION_ENABLED` switch (NOT the global kill switch) + `VISION_MODEL_NAME` set; off by default → audited denial, no model call. Screenshot bytes never persisted (metadata-only audit in `screen_vision_events`); MIME/size validation pre-call. Operator go-live = pull a vision model + set the two env vars. Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **CHRONOS Smart Scheduling — free/busy + find-a-time (2026-06-27)** — calendar CRUD → intelligence. New `app/chat_scheduling.py` (routed CHRONOS, short-circuits before the calendar handler). *"when am I free this week?"* / *"find 30 min tomorrow afternoon"* → READ-ONLY open-slot search across all calendars (weekdays 9–17 local, daypart-aware, busy subtracted from the user's own events; no new OAuth scope). *"schedule 30 min with sam@x next tue"* → finds the earliest slot then STAGES it via the new `chat_calendar.stage_create` (the existing confirm-before-write path) — "confirm" books it. Free/busy reads via the new shared `chat_calendar.gather_events_window` (`gather_day_events` now delegates to it). Booking FAILS CLOSED behind `calendar_write` + `CALENDAR_EXECUTION_ENABLED` (read works even when writes are off). Booking names the default calendar + offers a redirect when >1 is connected; `handle_calendar_turn` honors a confirm-time provider redirect ("confirm but on google" re-points a staged CREATE; update/delete ignore it). Traces `chat_scheduling_*`. `verify_chat_scheduling.py` added (+ calendar verify Part G for the redirect). **Calendar write disambiguation hardening (2026-06-27, found in use):** broadened delete detection so follow-up cancel phrasings ("cancel/delete/remove that|this meeting", "sorry lets cancel that meeting", "call off the meeting") route to the governed delete instead of the LLM (which had HALLUCINATED a cancellation that never ran); and **numbered candidate selection** — ambiguous update/delete targets now render a NUMBERED list (stashed as `select_target` in `calendar_pending_actions`), and a bare "2"/"cancel 2"/"the second one" (`detect_selection`, gated on a pending selection) picks that event → stages the real confirm-before-write. (calendar verify Part H). **Cross-provider write-target resolution (2026-06-27, confirmed in use):** a provider-less update/delete with >1 connected calendar now searches the target across ALL of them (`_resolve_target_across` → `_stage_one_write` / numbered provider-tagged `select_target`; the pick acts on the event's OWN calendar). Fixed a real failure where a Google-created event couldn't be cancelled because "cancel that meeting" only checked the default (Outlook) calendar. Named provider still pins one; create stays single. Calendar verify Part H rewritten to cross-provider. **Actionable numbered read lists (2026-06-27, confirmed in use):** calendar reads now render NUMBERED (provider-tagged) and stash the listed events (`_stash_list_context`), so "cancel 4" / "reschedule 2 to 3pm" (`detect_list_action`, incl. "can you cancel 4 please") acts on read item #4 → confirm-before-write on that event's own calendar; bare "4" after a read asks cancel-or-reschedule. Fixed another hallucination (the general model invented a "#4" cancellation). Calendar verify Part I added. (*Follow-up since RESOLVED 2026-06-27 — see the next bullet, "Per-user default WRITE calendar".*) Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **Per-user default WRITE calendar (2026-06-27)** — closes the above follow-up. A hint-less CREATE used to always land on `primary`; now the user can pick a specific calendar WITHIN a provider and unnamed creates go there. New `user_calendar_defaults` table (`(user_id, provider_name) → calendar_id+name`, complements `user_provider_defaults` which picks the provider). Chat-set via CHRONOS: "make Work my default calendar" / "set my default calendar to Work" / "use my Work calendar as default" → resolves the name against the provider's writable calendars (exact-wins, refuse-on-ambiguous, same governed read as a named create) and stores id+name; "clear my default calendar" reverts to primary. Used in `chat_calendar._prepare_write` (create branch: no NL hint → `get_default_calendar`, no provider call since id+name are stored) AND in `chat_scheduling._book_first` (a booked free slot honors it too). A calendar named in the message still overrides; provider-only defaults ("make google my default calendar") stay the `provider_defaults` path (no calendar name → returns None here, no collision). New `chat_calendar.detect_calendar_default_command`/`handle_calendar_default_command` wired in `chat.py` AFTER the provider-default block, BEFORE the calendar handler. Traces `chat_calendar_default_set`/`_cleared`. Calendar verify Part J added → **all 21 verify scripts PASS**; live `/chat` smoke green (set→governed-fail-soft for a connector-less user, clear, provider-default still routes to Cora, plain read not mis-detected). Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **Daily Briefing composite (2026-06-27)** — first cross-agent composite: "brief me on my day" → one READ-ONLY digest of today's schedule (all calendars) + inbox highlights (all mailboxes) + a news headline rundown. New `app/chat_briefing.py` short-circuits the chat pipeline before the single-domain inbox/calendar handlers (returned as Cora). Reuses the existing GOVERNED cross-provider reads via two new helpers (`chat_calendar.gather_day_events`, `chat_inbox.gather_inbox_highlights`) + `news_briefing.gather_briefing`; each section fails soft independently. No writes/sends, no DGX dependency, no UI/env change. Counts-only trace (`chat_daily_briefing_generated`). LIVE-smoke-verified via real `/chat`. Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **Model-driven Agent Runtime + Cora Configuration UI (2026-06-27)** — an opt-in reason→act→observe agent loop (`app/agent_runtime.py`) ADDED alongside the deterministic chat router (unchanged, still primary). 5 phases: tool-calling kernel → durable runs (`agent_runtime_runs` table) → hub-and-spoke delegation (orchestrator-only `delegate_to` → FORGE/PULSE/SIGNAL/CHRONOS spokes with `allowed_agents`-scoped tools; reuses `agent_delegations`; worker-driven + non-blocking via `POST /chat/agent/async` + the worker `agent_run` handler) → parallel fan-out (`asyncio.gather` + per-run semaphore) → review-only staging (stages drafts/proposals via the `internal_action` tools; **never sends email / writes a calendar**). Endpoints `POST /chat/agent`(+`/async`), `GET /chat/agent/runs/{id}`, `GET /chat/agent/config`. Three independent safety gates; all behind off-by-default `AGENT_*` flags (currently ALL ON: `AGENT_RUNTIME_ENABLED`/`AGENT_DELEGATION_ENABLED`/`AGENT_WRITE_ENABLED`, `DGX_CHAT_MODEL_NAME=cora-qwen3:4b`). UI: a **Cora Configuration** tab in Admin Console (flag status + a run-the-agent panel showing the tool/delegation trace) + an operator-added **Memories** tab. `verify_agent_runtime.py` (24 assertions) + live end-to-end on qwen3:4b (web_search turn, ATLAS→PULSE delegation, staged draft). Merged to `main` (@ `1279b77`). Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **Independent evaluator spoke — Phase 6 (2026-06-28)** — the generator/evaluator split (from the *Loop Engineering* paper). When `AGENT_EVAL_ENABLED` is on (default OFF), a FINISHED top-level run gets one adversarial review pass: a separate agent identity (assume-broken, no praise, **NO tools**) judges the answer + staged artifacts against the goal and returns a verdict (`pass`/`concerns`/`fail` + reasons + summary). **Review-only + advisory** — no tools, no external effects; it never sends/writes/gates execution, it only attaches a verdict to the run. `agent_runtime.evaluate_run` + `_render_eval_input` + robust `_parse_verdict` (malformed reply → `concerns`, never a silent pass); new `agent_runtime_runs.evaluation` JSONB column; optional independent judge model via `DGX_EVAL_MODEL_NAME` (falls back to the chat model). Surfaced in the sync `/chat/agent` response, in `GET /chat/agent/runs/{id}`, in `/chat/agent/config` (`eval_enabled`/`eval_model`), and in the UI (Evaluator status pill + a verdict card in the Agent result and the Runs detail). This is the front-half of backlog #1 (confirm-as-interrupt). `verify_agent_runtime.py` Part H (**39 assertions total**) PASS. Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **Agent Runs / task-manager view (2026-06-28)** — a **Runs** sub-tab under Cora Configuration (alongside the new **Agent** sub-tab) that makes multi-agent behavior visible: a list of recent runs (owner-scoped) → click a run to inspect its full step trace AND the orchestrator→spoke **delegation tree** (each ATLAS→spoke hop with the spoke's OWN step trace + answer embedded). New read-only endpoint `GET /chat/agent/runs` (list) + the existing `GET /chat/agent/runs/{id}` enriched with a `delegations` array. Correlation needs NO schema change: each delegation stamps its orchestrator's run id into `input_payload._parent_run_id`, so the tree rebuilds even for the sessionless runs the panel creates. `verify_agent_runtime.py` extended (Part G, **31 assertions total**) PASS. Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.
- **CHRONOS Calendar CRUD v1.0–v1.4** (2026-06-25) — chat-native full calendar CRUD (calendar analog of the v2.7 inbox assistant). Real Google/Microsoft adapters (`calendar_adapters.py`); `chat_calendar.py` with a fail-closed read gate (`calendar_read` flag) + stricter write gate (`calendar_write` flag **plus** the dedicated `CALENDAR_EXECUTION_ENABLED` master switch — NOT the global kill switch, which email/integration governance requires false). NL field extraction (DGX, regex fallback) + **confirm-before-write** (staged in `calendar_pending_actions`, fires only on "confirm"); past-aware read windows + recurring-series dedup + all-calendar aggregation + local-time render; cross-calendar writes (update/delete → event's own calendar; create → primary by default, or a **named calendar via NL** as of v1.5 — `_extract_calendar_hint` + `_resolve_calendar`, exact-name-wins, refuse-on-ambiguous); `_resolve_target` refuses-on-ambiguous (never deletes the wrong event); invites sent (`sendUpdates=all`); audited (`calendar_access_events`) + traced. **LIVE** — create/edit/cancel verified end-to-end. Full detail in [`AIOS_CORE_ARCHITECTURE.md`](./AIOS_CORE_ARCHITECTURE.md) §9.

- **Inference moved to vLLM/gpt-oss-120b + backend consolidation (2026-06-28 → 2026-06-29)** — Cora's text-gen now runs on an OpenAI-compatible **vLLM** server (gpt-oss-120b on the DGX, `spark-a84c:8000/v1`) instead of the 4B Ollama. Backend-selectable + reversible (`app/llm.py`): `DGX_CHAT_BACKEND` (chat + the two fact-extractions, `3957d35`) and the INDEPENDENT `DGX_AGENT_BACKEND` (the **agent-runtime tool loop + evaluator**, `77451cc`/`bb6f1fd`); `agent_runtime._chat(backend,…)` translates the canonical Ollama-shaped thread ↔ `/chat/completions` (`_to_openai_messages` synthesizes tool_call ids; `_normalize_openai_response` maps back). Then ALL remaining text-gen (summarize / news-briefing / email-draft / agent-test-response, `ae234b1`) routed through `llm.generate_text` too — so `DGX_CHAT_BACKEND` governs the whole app; only embeddings (`nomic-embed-text`) + screen vision (`qwen2.5vl`) stay Ollama-only. Code default stays `ollama` (fail-safe). ⚠️ The DGX `vllm-oss` container MUST be launched with `--enable-auto-tool-choice --tool-call-parser openai` or tool calls don't parse. Auto-memory `dgx_inference_backends`.
- **Confirm-as-interrupt OUTWARD half + evaluator-gated approval (2026-06-28 → 2026-06-29)** — completes backlog #1's machinery. OUTWARD (`c101eef`): on approve, IF `AGENT_EXECUTION_ENABLED` is on (compound gate, default off), `resolve_interrupt` fires the staged **calendar CREATE** through the existing gated `_write_gate → adapter.create_event` path (email never sent; calendar still needs `CALENDAR_EXECUTION_ENABLED` + per-provider `calendar_write`). Evaluator-gated approval (`8fe9f1b`, ties Phase 6 + 7): new `AGENT_EVAL_GATE_ENABLED` — approving a run whose evaluator verdict is `fail` is refused (HTTP 409, fires nothing) unless `override=true`; UI InterruptCard shows the verdict + an "Override & approve" on 409. `verify_agent_runtime.py` Parts J–L → **71 assertions**. Live-exercised on gpt-oss-120b (staging run → real verdict → pause → gate). Auto-memory `agent_runtime_build`.
- **Global news_article out of chat recall (2026-06-30, `d2adc0e`)** — the news-briefing pipeline stores every article as a `scope_type='global'` memory; **743 of 774 globals (96%) were `news_article`**, burying real memories past the 5-memory injection cap. Both recall paths now exclude `global AND type='news_article'` — `scribe.search_memory` (keyword) + `memory.embeddings.semantic_search` (vector, shared `scope_clause` → both sub-queries). Curated globals (`global_architecture_doc`, `workspace_knowledge`) + a user's OWN saved news still recall; the briefing is unaffected (reads articles by type from `knowledge_sources`). New DB-backed `verify_memory_recall_scoping.py` (keyword deterministic + semantic on live pgvector) PASS. *Surfaced, NOT done (operator decision):* 3 personal facts mis-scoped to global (wife/dog — re-scope to owner, account ambiguous) + test-junk `workspace_knowledge` globals — see next_session_prompt.md "Operator-only loose ends".
- **Calendar UPDATE/DELETE firing + LIVE outward fire confirmed (2026-06-30, `0ff4f44`)** — closes the last two agent-runtime backlog items. The agent fired CREATE only; it now also stages + fires **UPDATE** and **CANCEL** via two review-only staging tools (`chronos_update_calendar_event`/`chronos_cancel_calendar_event`, seeded internal_action/CHRONOS) → `_collect_staged` `calendar_update`/`calendar_delete` → `_fire_staged` → `chat_calendar.agent_fire_calendar_update`/`agent_fire_calendar_delete` (re-check `_write_gate('update'/'delete')`, fail-closed, never raise). `InterruptCard`/`types.ts` render the new types. `verify_agent_runtime.py` Part M → **78 assertions** (A–M); Part H "eval off" check made hermetic. **The full agent approve→fire path (`resolve_interrupt → _fire_staged`) was LIVE-CONFIRMED** against the real google_calendar — create→update→delete (+ cleanup delete) all `allowed=t`/`agent approve … ok` in `calendar_access_events`, calendar left clean, `AGENT_EXECUTION_ENABLED` armed IN-PROCESS only (persistent flag stayed false, no restart). (A governed calendar READ tool — `chronos_list_calendar_events` — shipped this session; see the voice-readiness entry below.) Auto-memory `agent_runtime_build`.
- **Voice-first UI readiness — 6 capabilities (2026-06-30, on `main` @ `aebc510`)** — a push to make agents/capabilities solid before a voice-first UI (ranked backlog in `VOICE_UI_READINESS.md`). Shipped: **`/chat` SSE streaming** (`a2721d8` — opt-in `ChatRequest.stream` → `meta/delta/done` SSE frames; backend-selectable `llm.stream_text`; the post-LLM tail factored into a shared `_finalize`/`_emit_chat_trace` closure; `sendChatStream` fills the bubble live; `X-Accel-Buffering:no`); **agent calendar READ tool** (`8993ff5` — `chronos_list_calendar_events`, governed read-only/CHRONOS-scoped, so the agent discovers an `event_id` from NL instead of needing one pasted; reuses `_read_gate`/`resolve_read_window`/`_read_one_calendar`); **spoken confirm-as-interrupt** (`ad0466f` — `agent_runtime.resolve_pending_for_session` + `POST /chat/agent/confirm` resolve a `waiting_user` run from a NL yes/no via `classify_confirmation`; a speakable `confirmation_prompt` baked into the interrupt payload; eval-gate + execution-gate still apply); **FORGE = real codebase/infra inspector** (`bd029d9` — it already OWNED the filesystem tools but its frozen seed prompt said "never calls tools"; rewrote it tool-aware via an idempotent no-clobber startup migration `registry._ensure_prompt_revision`; also fixed a latent crash — `_dispatch_read_only` now forwards only advertised args, a model-invented `line_start` crashed the MCP read — and added `read_file` line-range paging + raised the observation cap 4000→12000); **PULSE web-aware** (`548d382` — prompt no longer claims "no live web access" while the governed `web_search` tool IS wired; same migration helper, generalized to also fire on the stale phrase across operator-edited versions, preserving routing keywords, PULSE v6→v7); **speakable (TTS) reply mode** (`5386f31` — opt-in `ChatRequest.speakable` → no-markdown style instruction + `app/speakable.to_speakable` normalizer; default off, text UI unchanged). 6 new `verify_*` scripts; each live-confirmed on vLLM/real providers. **Remaining P1 (next session):** semantic routing fallback (#6), memory data-cleanup + spoken disambiguation (#7). **Audit surfaced (operator):** n8n is not deployed (FORGE automation-executor path deferred), `mcp-postgres`/`mcp-github` are placeholder images, the Planner is a template stub.
- **Chat experience + several user-reported fixes (2026-06-28 → 2026-06-29)** — memory: natural "remember this" persists + no fake saves (`5da4955`), hybrid recall via RRF so exact-term hits aren't buried (`552e920`), concise second-person answers (`5ce1877`/`5a71604`), prompt-cache + keep-warm latency fixes (`d036210`/`d541964`), and `delete/update memory <id>` accepts the 8-char short id `show memories` prints (`08ff3cd`). Email drafts no longer signed with an internal agent codename ("Best regards, SIGNAL" → the user's name / "Cora - the AI Assistant"; shared `signal_tools.normalize_email_signoff`, both draft paths, `2b3e51a`). Inbox: "what do I have in my outlook that is unread" now routes to the inbox handler + filters unread per provider (Gmail `is:unread`, Outlook `$filter=isRead eq false`, `df63b73`) — was falling through to the general LLM. New verify scripts `verify_chat_signal_signoff.py` + `verify_text_gen_backend.py`; `verify_chat_memory.py`/`verify_chat_inbox.py` extended. The non-admin agent panel also shipped (`64891de`).

**Verified end-to-end:** chat flow, memory create/search, tool routing, plan creation, job queue + worker execution, delegations, semantic retrieval, workspace context, web_search (PULSE), URL/PDF/news ingest, scheduled news refresh, agent versioning + test harness, SIGNAL/CHRONOS routing, live PULSE DGX turn, calendar CRUD gates (fail-closed + dedicated `CALENDAR_EXECUTION_ENABLED` master gate + LIVE create/edit/cancel verified end-to-end), Tier-2 screen vision (fail-closed gate + image validation + no-image-persist privacy, DGX call monkeypatched), per-provider OAuth redirect derivation, Outlook inbox+calendar LIVE (real MS account), the in-app execution kill-switch override (calendar/screen-vision admin-toggleable; external_execution env-locked), multi-provider command disambiguation (provider-word tolerance + per-user default provider), and 33 standalone verify scripts (`scripts/verify_*.py`, incl. `verify_agent_runtime.py` = 78 assertions Parts A–M, `verify_memory_recall_scoping.py` DB-backed keyword+semantic, and the 6 voice-readiness scripts `verify_chat_streaming`/`verify_chat_calendar_read`/`verify_chat_confirm`/`verify_agent_forge`/`verify_agent_pulse`/`verify_speakable`) all PASS, plus the agent-runtime calendar CREATE/UPDATE/DELETE fire path LIVE-confirmed on the real google_calendar (2026-06-30). **Plus the model-driven Agent Runtime: live end-to-end on gpt-oss-120b/vLLM (read-only web_search turn → grounded answer; staging run → evaluator verdict → pause → gate) as of 2026-06-29.**

## 10. Next Best Steps

As of 2026-06-27. Everything in the §9 changelog is DONE — do not re-suggest shipped modules.

**DONE (breadcrumbs — detail in §9):**
- **Confirm-as-interrupt — INTERNAL half — Phase 7 (2026-06-28)** — backlog item #1's safe front-half, shipped. Behind `AGENT_INTERRUPT_ENABLED` (default OFF), a top-level run that STAGED a review-only artifact pauses at `status='waiting_user'` (`_collect_staged` + `_pause_run`; completed_at stays NULL) with a pending `interrupt` payload on the new `agent_runtime_runs.interrupt` column. `POST /chat/agent/runs/{id}/decision` (`resolve_interrupt`, owner-scoped + atomic `FOR UPDATE`) records approve→done / reject→cancelled and **fires NOTHING external** — the real send/calendar-write is the deliberately-deferred dangerous half (one swap in `resolve_interrupt` later, with the operator present). Surfaced in `/chat/agent` (`status`+`interrupt`), `GET /chat/agent/runs/{id}`, and the UI (`InterruptCard` Approve/Reject in the Agent result + Runs detail; async polling stops at `waiting_user` so the human can act). `verify_agent_runtime.py` Part I → **47 assertions** PASS; api/ui/worker rebuilt + healthy; `interrupt` column migrated; OpenAPI carries the decision path + fields. *To enable:* `AGENT_INTERRUPT_ENABLED=true` (needs runtime+write on) + `up -d`.
- **Async runs in the UI (2026-06-28)** — backlog item #5 (first sub-item), shipped. The Agent panel gained a **"Run in background"** button → `POST /chat/agent/async` (worker-driven, non-blocking) → `RunDetail` polls `GET /chat/agent/runs/{id}` every 2s until a terminal status, rendering live steps + the delegation tree + the evaluator verdict; an "updating…" badge shows while in flight. Pairs with the now-concurrent worker (a long background run no longer blocks others). UI-only (`sendAgentChatAsync`, `AgentAsyncResponse`, `RunDetail poll`, `isTerminal`); cora-ui rebuilt (`tsc -b` clean) + healthy; async route smoke (401 not 404). Remaining #5 sub-items (relocate app-config screens; open agent to non-admin) untouched.
- **Independent evaluator spoke — Phase 6 (2026-06-28)** — backlog item #6 below, shipped. Generator/evaluator split behind `AGENT_EVAL_ENABLED` (default OFF): a finished top-level run gets one adversarial, tool-less review (assume-broken, no praise) → `pass`/`concerns`/`fail` verdict + reasons, stored on the new `agent_runtime_runs.evaluation` column and shown in the Agent result + Runs detail. Review-only/advisory — NO external effects, does not gate execution (that's backlog #1). Optional independent judge model `DGX_EVAL_MODEL_NAME`. `verify_agent_runtime.py` Part H → **39 assertions** PASS; api/ui/worker rebuilt + healthy; `evaluation` column migrated; OpenAPI carries the new fields. *To enable:* set `AGENT_EVAL_ENABLED=true` (+ optionally `DGX_EVAL_MODEL_NAME`) and `up -d` cora-api + cora-worker.
- **Worker concurrency (2026-06-28)** — backlog item #3 below, shipped. `cora-worker` ran one job at a time (`process_one` claimed+awaited a single job), so a long `agent_run` blocked news refreshes / other runs (a Phase-3 side effect). Now a **bounded concurrent pool**: `process_one` is split into `run_claimed(job)` (post-claim body, never raises) + extracted `_fill_slots`/`_reap`/`_idle_wait`; `main()` claims up to `WORKER_MAX_CONCURRENCY` (default 3) jobs as tasks, refills a slot the moment one finishes, and keeps the heartbeat + 60s news-scheduler ticking alongside long jobs. Graceful drain on shutdown (`WORKER_SHUTDOWN_DRAIN_SECONDS`, default 30). DB-pool-safe (connections are per-query, not held across a run). New `verify_worker_concurrency.py` (DB-free, drives the pool helpers; 12 assertions) PASS; worker rebuilt + healthy (`max_concurrency=3`), `/worker/health` main heartbeat + news scheduler both ticking.
- **Agent Runs / task-manager view (2026-06-28)** — backlog item #2 below, shipped. A **Runs** sub-tab under Cora Configuration (Cora Configuration now has **Agent** + **Runs** sub-tabs): owner-scoped run list → run detail with the full step trace + the orchestrator→spoke delegation tree (each hop embeds the spoke's own trace + answer). New `GET /chat/agent/runs` (list); `GET /chat/agent/runs/{id}` now returns `delegations`. Tree correlation via an `input_payload._parent_run_id` stamp (no schema change), so it works for sessionless panel runs. `agent_runtime.list_runs`/`get_run_delegations` + `verify_agent_runtime.py` Part G (**31 assertions**) PASS; api/ui/worker rebuilt + healthy; route smoke (401 not 404, both paths in OpenAPI). *Possible follow-ups (only if asked):* poll/auto-refresh for in-flight runs; filter by status/agent; drill from a spoke node into its own run row.
- **Model-driven Agent Runtime + Cora Configuration UI (2026-06-27)** — opt-in reason→act→observe agent loop (`app/agent_runtime.py`), additive to the deterministic router. 5 phases (kernel → durable runs `agent_runtime_runs` → hub-and-spoke delegation reusing `agent_delegations`, worker-driven via `/chat/agent/async` → parallel `asyncio.gather` fan-out → review-only staging via `internal_action`). Endpoints `/chat/agent`(+`/async`,`/runs/{id}`,`/config`); off-by-default `AGENT_*` flags (all ON now); **Cora Configuration** + **Memories** tabs in Admin Console; `verify_agent_runtime.py` (24) + live on qwen3:4b. Merged to `main` @ `1279b77`. *Build backlog below.*
- **Per-user default WRITE calendar (2026-06-27)** — closes the "no per-user default calendar for writes" follow-up. `user_calendar_defaults` table ((user,provider)→calendar id+name); chat-set via CHRONOS ("make Work my default calendar" / "set my default calendar to Work"; "clear my default calendar" reverts). A hint-less CREATE (`chat_calendar._prepare_write`) and a booked free slot (`chat_scheduling._book_first`) now land on it instead of `primary`; a named calendar still overrides; provider-only defaults stay the `provider_defaults` path (no collision). `detect/handle_calendar_default_command` wired in `chat.py` after the provider-default block. Traces `chat_calendar_default_set`/`_cleared`. Calendar verify Part J → **all 21 verify scripts PASS**; live `/chat` smoke green. *Possible follow-ups (only if asked):* a UI control to pick/show the default; surface the current default in the calendar read header.
- **CHRONOS Smart Scheduling — free/busy + find-a-time (2026-06-27)** — `app/chat_scheduling.py` (routed CHRONOS, short-circuits before the calendar handler). READ-ONLY availability search ("when am I free…", "find 30 min tomorrow afternoon") across all calendars (weekdays 9–17 local, daypart-aware, busy subtracted from own events, no new scope) + book-on-confirm ("schedule 30 min with sam@x next tue" → stages the earliest slot via new `chat_calendar.stage_create`, the existing confirm-before-write path). New shared `chat_calendar.gather_events_window` (arbitrary window read; `gather_day_events` delegates to it). Booking FAILS CLOSED behind `calendar_write` + `CALENDAR_EXECUTION_ENABLED`; read works when writes are off. `verify_chat_scheduling.py` → **all 21 verify scripts PASS**; live `/chat` smoke = CHRONOS + fail-closed read for a connector-less user. *Possible follow-ups (only if asked):* true multi-attendee availability via Graph `getSchedule` / Google `freeBusy`; "book slot #2" selection from a listed find; configurable working hours/timezone-per-user.
- **Daily Briefing composite (2026-06-27)** — "brief me on my day" → one read-only digest (today's schedule across all calendars + inbox highlights across all mailboxes + a news headline rundown). New `app/chat_briefing.py` (detect + handle), wired as a chat short-circuit before the single-domain inbox/calendar handlers, returned as Cora. Reuses the governed cross-provider reads via new helpers `chat_calendar.gather_day_events` + `chat_inbox.gather_inbox_highlights` + `news_briefing.gather_briefing`; each section fails soft independently (gated/unconnected provider → clean "not available" note, briefing still renders). No writes/sends, no email-send unlock, no DGX dependency, no UI/env/switch change. Counts-only trace `chat_daily_briefing_generated`. `verify_chat_briefing.py` added (detection + composite render + fail-soft); **all 20 verify scripts PASS**; live HTTP smoke through real `/chat` returned the full briefing (Cora, real news headlines, schedule/inbox fail-soft for a connector-less throwaway user) — throwaway rows cleaned. *Possible follow-ups (only if asked):* an optional one-paragraph DGX narrative lead; a scheduled/delivered version (reuse the worker `news_feed_refresh` scheduler pattern) as an internal artifact.
- **Tier-2 Screen Vision GO-LIVE (2026-06-26)** — LIVE against the real DGX (`qwen2.5vl:latest`). Fixed 3 gaps: wired `SCREEN_VISION_ENABLED`+`VISION_MODEL_NAME` into `docker-compose.yml` (the `.env` line never reached the container — env comes from compose `${VAR}`); corrected the model name (`.env` had `qwen2.5-vl`, actual is `qwen2.5vl:latest`); added `options:{num_ctx:8192}` to `screen_vision._analyze` (default context OOM'd the DGX by ~0.3 GiB → HTTP 500). Enabled via the **app override** (env stays `false`; `runtime_switches` override = ON, toggle in Execution Switches panel). Live-verified: real image → grounded answer; audit metadata-only (screenshot never stored). 19 verify scripts PASS.
- **Cross-provider read aggregation (2026-06-26)** — a provider-less read now merges ALL connected providers: "what's on my calendar" → Google + Outlook events merged (tagged `[google]`/`[outlook]`, start-sorted); "show my emails" → Gmail + Outlook merged (tagged, newest-first via `_msg_dt` parsing both RFC-2822 + ISO dates). Named-provider reads + ALL writes stay single. `chat_calendar`/`chat_inbox`: `_resolve_read_providers` + per-provider `_read_one_*` (own gate+broker+audit, skipped-provider note) + `_handle_*_multi` + `_render_*_multi`; single path unchanged (`_handle_read_single`/`_handle_inbox_single`). Live-verified (3 cal events, 12 emails merged). 19 verify scripts PASS (added calendar §F + inbox §D).
- **Calendar day-question detection fix (2026-06-26)** — "what about this past tuesday what was on it" (no calendar/meeting anchor) was falling through to the generic LLM, which hallucinated "I don't have calendar access". `detect_calendar_command` now routes a schedule-query phrase ("what was on"/"anything on"/… via `_has_day_ref`) + a day reference to a calendar read; `resolve_read_window` resolves "past/last <weekday>" to the most recent past occurrence (and "next <weekday>" forward). Conservative (a phrase with no day → still not a calendar command). Live-verified (read the real Outlook event for that Tuesday). 19 verify scripts PASS. *Known design note:* a provider-less day query reads ONE provider (resolved default/most-recent), not Google+Outlook merged — possible follow-up.
- **Multi-provider command disambiguation (2026-06-26)** — provider-word tolerance ("what's on my **outlook** calendar" / "show my **google** inbox" now detected; `app/provider_defaults.strip_provider_adjectives` normalizes the message before phrase-matching, resolution still reads the original) + a per-user **default provider** (`user_provider_defaults` table; `_resolve_provider` → keyword → connected default → most-recent → fallback) settable via chat ("make outlook my default calendar"). New shared module `app/provider_defaults.py`, `chat.py` short-circuit. Live-verified on real Outlook+Google accounts. 19 verify scripts PASS.
- **In-app execution kill-switch control (2026-06-26)** — admins can toggle `CALENDAR_EXECUTION_ENABLED` + `SCREEN_VISION_ENABLED` from the app (Admin → Tools → Execution Governance → **Execution Switches**) via a DB override layered over the env defaults (no redeploy). `EXTERNAL_EXECUTION_ENABLED` is shown but **env-locked** (`set_switch` refuses it — turning it on breaks the email approval interlock). New: `runtime_execution_switches` table, `app/runtime_switches.py` (`effective`/`set_switch`/`clear_override`), `/admin/execution-switches` router, UI panel. `chat_calendar._write_gate` + `screen_vision._gate` now read `runtime_switches.effective(...)`. Table left empty = pure env defaults (no operational change). 18 verify scripts PASS + HTTP smoke test green.
- **Outlook GO-LIVE (2026-06-26)** — `outlook_mail` + `outlook_calendar` connected (real MS account, `Mail.Read`+`Calendars.ReadWrite`); flags ON (`outlook_mail/inbox_read`, `microsoft_calendar/calendar_read`, `microsoft_calendar/calendar_write`); `CALENDAR_EXECUTION_ENABLED` already on. Live-verified: inbox read (10 msgs), calendar read (4 events), calendar write round-trip (throwaway create→delete, cleaned up). Send hard-disabled (`supports_send=false`). **Bug fixed during go-live:** `provider_connectors.update_connector` (admin `PATCH /providers`) force-zeroed `supports_calendar_create/update` on every connector edit — toggling a connector's `enabled` in the UI silently broke the calendar write gate for BOTH google + microsoft. Narrowed the guard to email send only; calendar caps now preserved (gated by flag + switch, not the capability column). **Also reverted** operator-enabled execution seeds (`send_email`/`create_calendar_event` ×4) back to fail-closed — not part of the read/calendar go-live, must stay off (nothing had executed: dry_run + kill switch + interlock all held). All 17 verify scripts PASS.
- **Per-calendar NL targeting for CREATE (2026-06-26, CHRONOS Calendar CRUD v1.5)** — "schedule X on my Work calendar" now routes the new event to that calendar (exact-name-wins, refuse-on-ambiguous, default primary = unchanged zero-provider-call path). Closes the v1.3 create-only-`primary` limitation. All 17 verify scripts PASS.
- **Live inbox read (2026-06-25)** — gmail connected (`gmail.readonly`+`gmail.send`), `gmail/inbox_read` flag ON, gate `allowed=True`; list/summarize/sender-scoped (`from:`) search live-verified; send hard-disabled. Outlook later: connect `outlook_mail` OAuth (`Mail.Read`) + enable its `inbox_read` flag.
- **Live calendar reads + writes (2026-06-25)** — google_calendar connected; `calendar_read`+`calendar_write` flags ON + `CALENDAR_EXECUTION_ENABLED=true` → create/edit/cancel LIVE (confirm-before-write). To DISABLE writes: `CALENDAR_EXECUTION_ENABLED=false` (restart) or turn off `calendar_write`. Outlook later: connect `outlook_calendar` OAuth + enable its flags (same switch).
- **Tier-2 Screen Vision v0.1 (2026-06-26)** — code-complete, fail-closed, awaiting operator go-live (see open action below). All 17 verify scripts PASS.

**Open operator actions:**
- **n8n `cora-health` webhook** — create the workflow (tool `n8n_health_check` 404s until it exists). Needs the n8n editor; surface it to the operator.
- **(optional) `DROP TABLE news_sources;`** — deprecated code path removed in v2.6; table remains in DB only. Destructive — confirm with operator first.

**Next BUILD module — Agent Runtime backlog (roughly by value/risk; operator picks):**
1. **Real external execution (the big one)** — ◑ INTERNAL HALF SHIPPED 2026-06-28 (Phase 7, see DONE breadcrumbs / §9): a top-level run that staged something now PAUSES at `waiting_user` (behind `AGENT_INTERRUPT_ENABLED`, default off) and `POST /chat/agent/runs/{id}/decision` (approve/reject, owner-scoped) resumes it, **recording the decision only — nothing fires**. UI: an Approve/Reject card in the Agent result + Runs detail. **OUTWARD HALF + EVALUATOR GATE NOW SHIPPED (2026-06-28 → 2026-06-29, `c101eef`/`8fe9f1b`):** on approve, IF `AGENT_EXECUTION_ENABLED` is on (default off), `resolve_interrupt` FIRES the staged calendar CREATE through the existing gated `_write_gate → adapter.create_event` path (email never sent); and `AGENT_EVAL_GATE_ENABLED` blocks an approve on a `fail` verdict (HTTP 409) unless `override=true`. `verify_agent_runtime.py` Parts J–M (78 assertions). ✅ **FULLY DONE (2026-06-30, `0ff4f44`):** the agent now fires calendar CREATE **+ UPDATE + DELETE** (two new staging tools → `agent_fire_calendar_update`/`_delete`, same gates), and the LIVE outward fire was confirmed end-to-end on the real google_calendar (create→update→delete, calendar left clean; `AGENT_EXECUTION_ENABLED` armed in-process only). `EXTERNAL_EXECUTION_ENABLED` stays false + email send hard-disabled. (Optional follow-up: a governed `chronos_list_calendar_events` read tool for autonomous NL targeting.)
2. ~~**Runs / task-manager view**~~ — ✅ SHIPPED 2026-06-28 (see DONE breadcrumbs above / §9). The **Runs** sub-tab under Cora Configuration renders the orchestrator→spoke tree, step traces, and run history.
3. ~~**Worker concurrency**~~ — ✅ SHIPPED 2026-06-28 (see DONE breadcrumbs above / §9). The worker now runs a bounded concurrent pool (`WORKER_MAX_CONCURRENCY`, default 3); a long agent run no longer blocks news refreshes / other runs.
4. ~~**Model reliability**~~ — ✅ SHIPPED 2026-06-29. The agent loop (+ all text-gen) now runs on **gpt-oss-120b via vLLM** (`DGX_AGENT_BACKEND=openai`), addressing the 4B "spokes answer from memory" problem. Config + the backend-bridge code (`77451cc`/`bb6f1fd`/`ae234b1`).
5. **Smaller polish** — ✅ *async-in-UI shipped 2026-06-28*; ✅ *agent panel opened to non-admin users 2026-06-28 (`64891de`)*. Still open: optionally relocate the app-config screens under Cora Configuration; plus newer code-only items — global-memory recall noise (some demo/global memories dilute personal recall) and `/chat` SSE streaming.
6. ~~**Independent evaluator spoke**~~ — ✅ SHIPPED 2026-06-28 (see DONE breadcrumbs above / §9). Generator/evaluator split behind `AGENT_EVAL_ENABLED` (default OFF): an adversarial, tool-less review pass over a finished top-level run → `pass`/`concerns`/`fail` verdict, advisory/review-only, stored on `agent_runtime_runs.evaluation` and shown in the UI. *Next step toward item 1:* ✅ SHIPPED 2026-06-29 (`8fe9f1b`) — evaluator-gated approval: the verdict shows on the `waiting_user` approval card and `AGENT_EVAL_GATE_ENABLED` blocks an approve on a `fail` verdict (HTTP 409) unless overridden.

Older still-open candidates (pre-agent-runtime, if preferred): the governed email SEND go-live (the one major capability still deliberately hard-disabled — highest impact + sensitivity, keep behind explicit per-message approval even when on); a scheduled/delivered Daily Briefing (reuse the worker `news_feed_refresh` scheduler pattern as an internal artifact); true multi-attendee availability for scheduling (Graph getSchedule / Google freeBusy).

## 11. Do Not Repeat

- **Don't ask clarifying OR direction-choosing questions** before acting — this now explicitly includes `AskUserQuestion`-style option menus. Proceed autonomously from context, report tersely. Don't write pre-action plans, decision lists, or "Now doing X…" interim narration between tool calls. (Durable memory `feedback_no_questions.md`; user reinforced again 2026-05-28 with "do not ask me again". The one carve-out is confirming genuinely destructive/irreversible actions.)
- **Keep `HANDOFF_SESSION.md` current** — user asked (2026-05-28) to update this brief after each completed item/module. Update the relevant sections (state, files, decisions, next steps) when a module lands; this is a manual workflow step, not a hook.
- **Don't reintroduce `select_subagent` into `forge.py`** — multi-agent selection lives in `app/agents/routing.py` now. Add new subagents there, not in forge.
- **Don't recreate the postgres volume.** It's `cora-stack_postgres_data`, declared external in compose. Recreating would delete all data.
- **Don't change `cora-stack/docker-compose.yml`** unless explicitly asked. We deliberately left it alone; user will clean up later.
- **Don't try to use stdio transport for MCP** — v0.1 is HTTP JSON-RPC only. The placeholder servers and filesystem server explicitly speak both REST + JSON-RPC at the same root URL.
- **Don't add new admin features as user-facing pages** — admin menu items are gated by `canAdmin && !impersonating` in Sidebar.
- **Don't replace keyword memory search with semantic.** Hybrid + keyword fallback is intentional.
- **Don't tighten governance further on the per-memory `POST /memory/{id}/embed` endpoint** — it's intentionally available to any authenticated user. `POST /memory/embed-missing` IS admin-only.
- **Don't normalize `workspace_id IS NULL` rows** by writing migrations to back-fill — legacy NULL is treated as "visible everywhere" by design, both for memory and knowledge_sources.
- The earlier session's `HANDOFF-2026-05-27.md` is stale; trust this brief.

## 12. Assumptions and Unknowns

**Confirmed:**
- Postgres container is on the pgvector image and on `cora-internal` with alias `postgres` (the original DATABASE_URL form `postgresql://cora@postgres:5432/cora` still resolves)
- Schema migrations run idempotently on every cora-api start
- Default workspace `cora-ai-os` is seeded with description; selector defaults to it
- Knowledge sources sub-router is mounted at `/knowledge/sources` separately from the workspace-prefixed list endpoint
- All UI changes compile (last tsc exit = 0, after PULSE UI changes)
- All backend changes syntax-check (last py_compile exit = 0, incl. pulse.py/routing.py/chat.py)
- `python-multipart==0.0.20` installed in the running image (verified 2026-05-28)
- pgvector cutover DONE: postgres on `pgvector/pgvector:pg16`, `vector` extension present
- PULSE deployed: seeded as active v1, images rebuilt, containers healthy, routing unit-tested
- `EMBEDDING_MODEL_NAME=nomic-embed-text` is set and responding (768-dim) from `spark-a84c:11434`

**Assumed:**
- An admin user exists (someone ran `POST /auth/register` first)
- `JWT_SECRET` is set in `.env` (otherwise auth endpoints return 503)
- DGX endpoint reachable for LLM calls (embedding endpoint confirmed reachable; generate endpoint assumed)

**Unknown / needs verification:**
- Whether n8n's `cora-health` webhook workflow has been created (tool `n8n_health_check` will 404 until it is)

**Resolved 2026-06-12:** live PULSE turn verified end-to-end inside cora-api (routing → DB prompt v6 → real DGX generation on `cora-qwen3:4b` → 2305-char response); cora-stack postgres service removed (v2.6).

**Newly confirmed 2026-05-28 (were unknown in prior brief):** cora-api rebuilt with python-multipart + feedparser; pgvector cutover succeeded (`vector` extension present); `EMBEDDING_MODEL_NAME=nomic-embed-text` set + responding; memory entries embedding into the pgvector column; markdown + news_article knowledge sources exist. A demo "Hacker News" news source (feed `https://hnrss.org/frontpage`) is registered in the `cora-ai-os` workspace with 3 ingested+embedded articles — safe to delete via the News UI if a clean slate is wanted.

## 13. Suggested First Message for the New Session

> **The canonical, current first message now lives in [`next_session_prompt.md`](./next_session_prompt.md)**
> (kept current; updated 2026-06-30 — `main` @ `831cbf8`, all inference on vLLM/gpt-oss-120b, 33 verify
> scripts incl. `verify_agent_runtime.py` = 78 assertions; the voice-first UI readiness push shipped 6
> capabilities, P1 #6/#7 remain). Read that first, then this brief's §9-backed
> What's Completed + §10 backlog for detail. **The paragraphs below are retained for reference but predate
> the 2026-06-28→29 vLLM/agent-runtime work — trust `next_session_prompt.md` + What's Completed over them.**
>
> Continuing the Cora AI OS build. Read **`HANDOFF_SESSION.md`** (repo root) as the single source of truth; full per-module detail is in the **`AIOS_CORE_ARCHITECTURE.md`** §9 changelog — do not restart or re-summarize those modules (shipped, verified, deployed). The stack is up and healthy.
>
> **Most recent (2026-06-27, two modules):** (1) the **Daily Briefing composite** — "brief me on my day" → a read-only digest of today's schedule (all calendars) + inbox highlights (all mailboxes) + a news headline rundown (`app/chat_briefing.py`; reuses the governed cross-provider reads; returned as Cora). (2) **CHRONOS Smart Scheduling** — free/busy + find-a-time (`app/chat_scheduling.py`, routed CHRONOS): READ-ONLY open-slot search across all calendars ("when am I free…", weekdays 9–17 local, daypart-aware, busy subtracted from own events, no new OAuth scope) + book-on-confirm ("schedule 30 min with sam@x next tue" → stages the earliest slot via the new `chat_calendar.stage_create` confirm-before-write path). Plus calendar write-path hardening (confirm-time provider redirect, broadened delete detection, cross-provider delete, numbered/actionable read lists). All COMMITTED + pushed to a branch (NOT yet on `main`) — see git state below.
>
> The prior session shipped a lot (ALL 2026-06-26, LIVE against the real Google + Microsoft accounts + the real DGX, now **committed + merged into `main`** — commit `aef130a`): per-calendar NL CREATE targeting (CHRONOS v1.5); **Outlook go-live** (`outlook_mail` + `outlook_calendar` connected; inbox read + calendar CRUD live; send hard-disabled) + a latent-bug fix (`provider_connectors.update_connector` was zeroing calendar create/update caps on any connector edit); **in-app execution kill-switch control** (`runtime_execution_switches` + `/admin/execution-switches` + UI panel in Execution Governance); **multi-provider command disambiguation** (provider-word tolerance + per-user default provider, `app/provider_defaults.py`); a **calendar day-question detection fix** ("what was on this past tuesday"); **cross-provider read aggregation** (provider-less reads merge Google + Outlook for both mail + calendar); and **Tier-2 Screen Vision GO-LIVE** (real `qwen2.5vl:latest` on the DGX — see below). Each was verified live.
>
> **Git state:** the prior session's work is on `main` (`aef130a`). **The 2026-06-27 work (Daily Briefing + Smart Scheduling + calendar UX/bugfixes) is COMMITTED as `379c640`** ("Daily Briefing + CHRONOS Smart Scheduling + actionable calendar lists", 10 files +1439/−32) on branch **`feat/daily-briefing-scheduling-calendar-ux`**, **pushed to `origin`** (tracking set). **NOT yet on `main`** — left as a PR pending the operator's live verification (PR link: https://github.com/fpokrzywa/cora_os/pull/new/feat/daily-briefing-scheduling-calendar-ux). The deployed cora-api/worker already run this branch's code (built + all 21 verify green). **To advance main when verified:** `git push origin HEAD:main` (direct fast-forward; `gh` is NOT installed + no `GH_TOKEN`, so use plain `git`). Intentionally left uncommitted in the working tree: `.claude/settings.local.json` (permission-allowlist churn), the `.gitignore` edit, and the `prompt.txt` deletion (none are part of this work). `.env` gitignored (secrets safe). **Operator pre-merge checklist (test on the real Google+Outlook account):** (1) `what is on my calendar next week` → numbered list; (2) `cancel 4` → confirm names the right calendar → `confirm` → event actually gone from Google; (3) `when am I free this week`; (4) `reschedule 2 to <time>` → confirm; (5) `brief me on my day`.
>
> Key architecture facts — do not break: (1) calendar writes use the **dedicated `CALENDAR_EXECUTION_ENABLED` switch**, NOT the global `EXTERNAL_EXECUTION_ENABLED` (which must stay `false` — email/integration approval governance requires it); (2) the two execution switches `CALENDAR_EXECUTION_ENABLED` + `SCREEN_VISION_ENABLED` are now **app-toggleable** via a DB override (`app/runtime_switches.effective(...)` layered over the env default) — `external_execution_enabled` is deliberately NOT toggleable (env-locked); (3) email send stays hard-disabled (`supports_send` code-guarded + `send_email` flag fail-closed); (4) provider-less **reads** aggregate ALL connected providers, but named-provider reads + ALL writes stay single-provider (+ confirm-before-write).
>
> **Tier-2 Screen Vision is now LIVE** (`app/screen_vision.py` → real `qwen2.5vl:latest` on the DGX). State: `VISION_MODEL_NAME=qwen2.5vl:latest` (wired into `docker-compose.yml` cora-api/worker `environment:`); env `SCREEN_VISION_ENABLED=false` but the **app override = ON** (effective true) — toggle in Admin → Tools → Execution Governance → **Execution Switches**, or `DELETE /admin/execution-switches/screen_vision_enabled` to revert to env. `_analyze` sends `options:{num_ctx:8192}` so the model fits DGX memory (its default context OOM'd it). Live-verified end-to-end (real image → grounded answer; audit metadata-only, screenshot never stored).
>
> **No build module is queued — ask me what's next.** (Daily Briefing + CHRONOS Smart Scheduling both landed 2026-06-27; if you want a pick: the governed email SEND go-live, a scheduled/delivered Daily Briefing, or true multi-attendee availability for scheduling.) Open items, all operator-only (surface, don't do): n8n `cora-health` webhook (needs the n8n editor http://n8n.local.arpa; `n8n_health_check` 404s until created); optional `DROP TABLE news_sources` (dead since v2.6, destructive — confirm first). Possible polish (only if I ask): improve `screen_vision._PROMPT` after real screen-share use; decide how the per-user default provider should interact with cross-provider aggregation (today provider-less reads always aggregate); or `.gitignore` the churning `.claude/settings.local.json`.
>
> **Suggested skills:** `/verify` (manually confirm a change works in the running app); `/run` (launch + drive the UI); `/code-review` (review your diff before reporting done).
>
> Working rules (saved feedback): no clarifying or direction-choosing questions, no preamble/interim narration — execute and report tersely; confirm only before destructive/irreversible or outward-facing actions (writes to my real calendar/inbox — use clearly-labeled throwaway items with no attendees and clean them up; never recreate the `cora-stack_postgres_data` volume; don't edit `cora-stack/docker-compose.yml` unless asked). Keep `HANDOFF_SESSION.md` current as each module lands (update §9/§10/§13/§14, don't just append). Standard per-module workflow: edit → `python3 -m py_compile` + UI `tsc --noEmit` → `docker compose up -d --build cora-api cora-worker cora-ui` → run the relevant `scripts/verify_*.py` inside the cora-api container + a full-suite regression.

## 14. Compact Technical Summary

Cora AI OS, FastAPI + React + Postgres(pgvector) + n8n + DGX Ollama. Repo `/home/owner/cora-ai-os`. Compose services: cora-postgres (pgvector/pgvector:pg16, vol `cora-stack_postgres_data`), cora-api, cora-worker (same image, `python -m app.worker`), cora-ui, cora-searxng (web_search backend, on cora-proxy for egress), cora-redis, mcp-filesystem/postgres/github. Networks: cora-internal (internal=true, no egress) + cora-proxy (egress), both external. Persona=Cora, orchestrator=ATLAS, subagents=SCRIBE/FORGE/PULSE/SIGNAL/CHRONOS; ATLAS routes deterministically via `app/agents/routing.py` (`select_subagent`/`diagnose_routing`: most-keyword-matches wins, tie→specificity then FORGE→PULSE→SIGNAL→CHRONOS, else Cora; DB `metadata.routing_keywords` override Python constants; no autonomous loops). Idempotent migrations on every cora-api start. JWT auth, scoped data (user/global/system + workspace_id, NULL=legacy-visible). Chat pipeline: memory-intent → tool-intent → forge-intent → plan-intent → web-search-intent(PULSE/SearXNG) → ATLAS select → resolve prompt via shared `resolve_agent_prompt` (DB active-version preferred, Python fallback) → current-date preamble + workspace context (≤1500 chars) → hybrid memory (semantic+keyword) → LLM. Agent prompts are versioned via Agent Admin (`agent_versions` + metadata, activate/archive, `agent_version_*` traces) with a test harness (`/admin/agents/test-routing|test-response`). Governance: tools.allowed_agents + tool_execution_policies + risk_level; audit in tool_execution_logs + runtime_traces. Plans template-only; jobs queued + executed by worker (`FOR UPDATE SKIP LOCKED` + retries + heartbeat). Knowledge (unified `knowledge_sources` + linked `memory_entries`, content_hash dedup, scope-aware): manual/bulk/upload(.txt/.md/.markdown/.json 512KiB)/URL(HTML/plain/PDF via url_ingest)/URL-refresh, and news — `POST /workspaces/{id}/knowledge/news` + `news_ingest.py` → news_feed/news_article sources + memory, optional full-article-body fetch, registered feeds + background scheduled refresh (`news_feed_refresh` job + 60s worker scheduler, dup-prevented). Embeddings via Ollama `/api/embeddings` (nomic-embed-text 768d, ~8000-char cap per call), stored in `embedding vector(EMBEDDING_DIM)` else `embedding_json`. **Chunked embeddings:** long content split into `memory_entry_chunks` (4000/400), each embedded; `semantic_search` prefers chunk vectors (→ parent + matched chunk + source citation) then memory-level then keyword; retrieval_mode incl. `semantic_chunks`/`hybrid_chunks`; rebuild via `/memory/{id}/chunks/rebuild` + `/memory/chunks/rebuild-missing`. News briefing: `GET /knowledge/news/briefing` lists recent ingested news + aggregates, with optional bounded PULSE summary (no web fetch). UI: sidebar Chats/Memory + single gated Admin Console (tabbed); Knowledge tab has ingest forms + News Feed Manager + News Briefing. Old `news_sources`/`/news/*` code path REMOVED in v2.6 (DB table remains, droppable). cora-worker is on cora-internal + cora-proxy (egress for scheduled feed fetches); scheduler tick health surfaced on `/worker/health` (`news_scheduler` block); feed failures back off exponentially (`consecutive_failures`, cap 8× interval). Inbox read adapters are LIVE (v2.7: gmail/Graph read-only endpoints, token broker behind the fail-closed gate); gmail connected + `inbox_read` flag ON → list/summarize/sender-scoped (`from:`) search LIVE, send hard-disabled. **Calendar CRUD (v1.0–v1.4)** LIVE: `calendar_adapters.py` (Google `calendar/v3` + Graph) + `chat_calendar.py` (CHRONOS; `_read_gate`=`calendar_read` flag, `_write_gate`=`calendar_write` flag + the **dedicated `CALENDAR_EXECUTION_ENABLED` switch** as master gate — NOT the global `EXTERNAL_EXECUTION_ENABLED`, which email/integration governance requires false; NL extract + confirm-before-write via `calendar_pending_actions`; past-aware windows + series dedup + all-calendar aggregation; cross-calendar writes; `_resolve_target` refuses-on-ambiguous; `calendar_access_events` audit). google_calendar connected, flags ON, switch true → create/edit/cancel verified LIVE (2026-06-25). Routed CHRONOS. **v1.5 (2026-06-26): per-calendar NL CREATE targeting** — `_extract_calendar_hint` ("on/in/to my|the <name> calendar" / "calendar named <name>") + `_resolve_calendar` (lists writable calendars via new adapter `list_calendars`, exact-name-wins-over-substring, refuse-on-ambiguous, default primary = unchanged zero-provider-call path); confirmed create lands on the named `calendar_id` (Graph `create_event` now posts to `/me/calendars/{id}/events` for non-primary). **Outlook is LIVE (2026-06-26):** `outlook_mail`+`outlook_calendar` connected (real MS account), `inbox_read`/`calendar_read`/`calendar_write` flags ON → live inbox read + calendar CRUD verified; send hard-disabled. Go-live also fixed a latent bug: `provider_connectors.update_connector` (admin `PATCH /providers`) force-zeroed `supports_calendar_create/update` on every connector edit (broke the calendar write gate for google + microsoft when a connector's `enabled` was toggled in the UI); guard narrowed to email-send-only, calendar caps now preserved (gated by flag + `CALENDAR_EXECUTION_ENABLED`, not the capability column). The 4 live-execution seeds (`send_email`/`create_calendar_event`) stay fail-closed. **Tier-2 Screen Vision v0.1** (`app/screen_vision.py`, 2026-06-26): opt-in screenshot → LOCAL DGX vision model (Ollama `images`); UI `screenCapture.ts` (`getDisplayMedia`, single frame, no auto-capture) + "Share screen" button → `ChatRequest.screen_image` → `handle_screen_vision_turn` (FIRST chat short-circuit, routed Cora). Fail-closed `_gate` = dedicated `SCREEN_VISION_ENABLED` switch (NOT the global kill switch) + `VISION_MODEL_NAME` set; MIME/8MB validation pre-call; screenshot bytes NEVER stored (metadata-only `screen_vision_events`); OFF by default. **LIVE (2026-06-26):** real `qwen2.5vl:latest` on the DGX; `SCREEN_VISION_ENABLED`+`VISION_MODEL_NAME` wired into `docker-compose.yml` (cora-api/worker); enabled via the app override (env stays false); `_analyze` sends `options:{num_ctx:8192}` so the model fits DGX memory; live-verified (real image → grounded answer, audit metadata-only). **Execution kill-switch override (2026-06-26):** `runtime_execution_switches` table + `app/runtime_switches.py` (`effective`/`set_switch`/`clear_override`) layer a DB override over the env defaults; `chat_calendar._write_gate` reads `effective("calendar_execution_enabled")` and `screen_vision._gate` reads `effective("screen_vision_enabled")`. Admins toggle these two from the app (`/admin/execution-switches`; UI panel in Execution Governance). `external_execution_enabled` is registered but NOT manageable (env-locked; `set_switch` refuses it — `execution_guard` still reads settings directly). No override row ⇒ env default; table currently empty. **Multi-provider disambiguation (2026-06-26):** `app/provider_defaults.py` — `strip_provider_adjectives` lets `detect_inbox_command`/`detect_calendar_command` recognize "my **outlook** calendar"/"**google** inbox" (resolution still reads the original message); per-user `user_provider_defaults` table + `resolve()` make a keyword-less request prefer the connected default (settable via chat: "make outlook my default calendar") over most-recent; `chat.py` short-circuit `detect_default_command`/`handle_default_command`. **Cross-provider read aggregation (2026-06-26):** a provider-less read merges ALL connected providers — `chat_calendar`/`chat_inbox` `_resolve_read_providers` (named→[one]; else all connected) → `_handle_read`/`handle_inbox_command` dispatch ≤1→single (`_handle_read_single`/`_handle_inbox_single`, unchanged) vs >1→`_read_one_*` per provider (own gate+broker+audit) merged via `_render_*_multi` (events tagged by source, start-sorted; emails newest-first via `_msg_dt` parsing RFC-2822 + ISO). Named-provider reads + ALL writes stay single. **Daily Briefing composite (2026-06-27):** `app/chat_briefing.py` (`detect_briefing_command` + `handle_briefing_command`) — a chat short-circuit (before the inbox/calendar handlers, returned as Cora) that composes a READ-ONLY day digest: today's schedule (all calendars) + inbox highlights (all mailboxes) + a news headline rundown. Reuses the governed cross-provider reads via two new composite helpers `chat_calendar.gather_day_events` + `chat_inbox.gather_inbox_highlights` (each drives the existing `_resolve_read_providers`/`_read_one_*` gate+broker+audit+merge) and `news_briefing.gather_briefing` (DB read, no web fetch, no PULSE LLM call). Each section fails soft independently (gated/unconnected provider → "not available" note, briefing still renders). No writes/sends, no DGX dependency, no env/switch/UI change; counts-only trace `chat_daily_briefing_generated`. `verify_chat_briefing.py` added. **CHRONOS Smart Scheduling (2026-06-27):** `app/chat_scheduling.py` (`detect_scheduling_command` + `handle_scheduling_command`, routed CHRONOS, short-circuits before the calendar handler) — free/busy + find-a-time. READ-ONLY availability ("when am I free…", "find 30 min tomorrow afternoon") computes open slots from the user's own events across all calendars via the new shared `chat_calendar.gather_events_window` (per-provider read gate+broker+audit; `gather_day_events` delegates to it), subtracting busy intervals from working hours (weekdays 9–17 local, evening→20, all-day ignored, today clipped to now, starts rounded to :15, ≤6 ranges); daypart narrows the window; window parsed by reusing `resolve_read_window` (no-day-ref default tightened to +7d). Booking ("schedule 30 min with sam@x next tue") finds the earliest slot then stages it via new `chat_calendar.stage_create` (explicit fields → same write gate + `calendar_pending_actions` + confirm card as a chat create; a later "confirm" books through the unchanged `_handle_confirmation`); FAILS CLOSED behind `calendar_write` + `CALENDAR_EXECUTION_ENABLED` (free/busy READ works when writes are off). No new OAuth scope (own-events only; multi-attendee getSchedule/freeBusy = future). Traces `chat_scheduling_freebusy_requested`/`_slots_listed`/`_slot_staged`. `verify_chat_scheduling.py` added → **21 verify scripts total**, all PASS. **Per-user default WRITE calendar (2026-06-27):** new `user_calendar_defaults` table ((user_id, provider_name) → calendar_id+name) stores the specific calendar a hint-less CREATE targets within a provider, complementing `user_provider_defaults` (which picks the provider). `chat_calendar.detect_calendar_default_command`/`handle_calendar_default_command` (set/clear via chat: "make Work my default calendar" resolves the name against the provider's writable calendars exact-wins/refuse-on-ambiguous then stores id+name; "clear my default calendar" reverts to primary) wired in `chat.py` AFTER the provider-default block, BEFORE the calendar handler. `_prepare_write` (create branch, no NL hint → `get_default_calendar`, no provider call) and `chat_scheduling._book_first` (booked slot) honor it; a named calendar overrides; provider-only "make google my default calendar" stays the `provider_defaults` path (no calendar name → None here). Traces `chat_calendar_default_set`/`_cleared`; calendar verify Part J. **Model-driven Agent Runtime (2026-06-27):** `app/agent_runtime.py` — an opt-in reason→act→observe loop ADDED alongside the deterministic router (which is unchanged + primary). The model chooses tools; the loop dispatches them through the EXISTING `dispatch_tool` + `check_permission` governance. 5 phases: (1) kernel — DGX `/api/chat` tool-calling over a curated READ-ONLY catalog, budget `AGENT_RUNTIME_MAX_STEPS`; (2) durable runs — new `agent_runtime_runs` table (status machine + `messages`/`steps` JSONB), `GET /chat/agent/runs/{id}`; (3) hub-and-spoke delegation — orchestrator-only `delegate_to` spawns a registry specialist (FORGE/PULSE/SIGNAL/CHRONOS) as its own `allowed_agents`-scoped spoke run, reusing `agent_delegations`; minimal-context payload; worker-driven + non-blocking via `POST /chat/agent/async` → the worker `agent_run` handler; (4) parallel fan-out — concurrent spokes via `asyncio.gather` + `Semaphore(AGENT_DELEGATION_MAX_PARALLEL=3)`; (5) review-only staging — stages `internal_action` drafts/proposals (never sends/writes a calendar) behind three gates (flag + check_permission external-block + internal_action-only floor). Endpoints `/chat/agent`(+`/async`,`/runs/{id}`,`/config`). Flags (env, default false, all ON now, wired into compose for cora-api AND cora-worker): `AGENT_RUNTIME_ENABLED`/`AGENT_DELEGATION_ENABLED`/`AGENT_WRITE_ENABLED`/`AGENT_DELEGATION_MAX_PARALLEL=3`/`DGX_CHAT_MODEL_NAME=cora-qwen3:4b`. UI: a **Cora Configuration** tab in Admin Console (`CoraConfiguration.tsx` — flag status + run-the-agent panel with the tool/delegation trace) + an operator-added **Memories** tab. `scripts/verify_agent_runtime.py` (24 deterministic assertions) PASS; live end-to-end on qwen3:4b; merged to `main` @ `1279b77`. Deferred (dangerous): real external execution inside the loop via confirm-as-interrupt (`waiting_user`). (Full per-module detail for all of the above lives in `AIOS_CORE_ARCHITECTURE.md` §9.) Stack deployed + healthy as of 2026-06-27.
