// Default config for local/dev use. In the container this file is
// REGENERATED at startup from the CORA_API_URL env var (see Dockerfile /
// 40-cora-ui2-config.sh) — edits here only affect non-Docker serving.
window.CORA_UI2_CONFIG = { apiBase: "http://api.cora.local.arpa" };
