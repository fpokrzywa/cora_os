Search the codebase for "ATLAS router placeholder". Remove that placeholder code completely. Update the exact route file that contains it.

The /chat endpoint must call Ollama at:
POST http://spark-a84c:11434/api/generate

Using:
model: cora-qwen3:4b
prompt: request.message
stream: false

The response should use the "response" field returned by Ollama.

After changing it, rebuild the Docker image with no cache:
docker compose build --no-cache cora-api
docker compose up -d cora-api
